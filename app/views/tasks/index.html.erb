<h1>Roao's first view 🚀</h1>

<% @tasks.each do |task| %>
  <li>
    <%= link_to task.name, task_path(task) %> <%= task.completed ? '☑️' : '☐' %>
  </li>
<% end %>

<h3><%= link_to 'Add a new task', new_path %></h3>

